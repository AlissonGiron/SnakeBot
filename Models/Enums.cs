namespace Models
{
    public class Enums
    {
        public enum Axis
        {
            X,
            Y
        }

        public enum Sentido
        {
            Maior,
            Menor
        }

        public enum Direction
        {
            Left,
            Right,
            Up,
            Down
        }

        public enum NodeType
        {
            Chao,
            Comida,
            Snake,
            SnakeHead
        }
    }
}
